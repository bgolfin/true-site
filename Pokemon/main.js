/* const jumbotronTop = document.getElementById('jumbotronTop');
const firstHead = document.getElementById('firstHead');

console.log(jumbotronTop);
console.log(firstHead);

const rrrrMessage = document.createElement('p');
rrrrMessage.textContent = "There's still tickets to WIN for the Global GO Fest next year! Enter on our Competition Page";

//This is the same way as appendChild but in reverse
function addToBeginning(parent, toInsert) {
    const firstChild = parent.firstChild;
    parent.insertBefore(toInsert, firstChild);
}

addToBeginning(jumbotronTop, rrrrMessage); */